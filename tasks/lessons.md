# lessons

- Keep public web pages paired with matching SVG favicons and social metadata before considering them publishable.
- Social preview images (`og:image`, `twitter:image`) must be raster (PNG/JPG) at 1200x630; Twitter Cards and most Open Graph consumers do not render SVG. Keep SVG for the in-browser favicon only.
